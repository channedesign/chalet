<%= form_for(@pricing) do |f| %>
  <% if @pricing.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pricing.errors.count, "error") %> prohibited this pricing from being saved:</h2>

      <ul>
      <% @pricing.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :self_catered %><br>
    <%= f.text_field :self_catered %>
  </div>
  <div class="field">
    <%= f.label 'Price Per Weekend' %><br>
    <%= f.text_field :weekend %>
  </div>
  <div class="field hidden">
    <%= f.label :week_id %><br>
    <%= f.text_field :week_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
