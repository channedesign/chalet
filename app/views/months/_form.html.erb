<%= form_for(@month) do |f| %>
  <% if @month.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@month.errors.count, "error") %> prohibited this month from being saved:</h2>

      <ul>
      <% @month.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :name %><br>
    <%= f.text_field :name, class: 'form-control' %>
  </div>
  <div class="field hidden">
    <%= f.label :appartment_id %><br>
    <%= f.text_field :appartment_id %>
  </div>
  <div class="form-group">
    <%= f.submit @month.new_record? ? 'Create Month' : 'Update Month', class: 'btn btn-default' %>
  </div>
<% end %>
