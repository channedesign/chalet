<h1 class="text-center">Add Floor Plans to <%= @floor_plan.house.name %></h1><br><br>

<%= form_for @floor_plan, html: { multipart: true, class: 'dropzone dropzone-floor-plan' } do |f| %>
  <% if @floor_plan.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@floor_plan.errors.count, "error") %> prohibited this floor plan from being saved:</h2>

      <ul>
      <% @floor_plan.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="fallback">
    <%= f.file_field :image %>
  </div>

<% end %>

<%= link_to 'Back', floor_plans_path, class: 'btn btn-link' %>
